export type IPaletteColor = {
  blue: string;
  red: string;
  green: string;
  yellow: string;
  darkYellow: string;
};

export type ITheme = {
  primary: string;
  secondary: string;
  bgApp: string;
  fontFamily: string;
};
